# Python / Jupyter scratch only — the generated figures and outputs ARE committed
# (so a reviewer can see every graph and number without running anything)
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
# LaTeX build aux only
*.aux
*.log
*.out
*.toc
*.synctex.gz
.DS_Store
Thumbs.db

# respaldos de edicion: NUNCA publicar, son un archivo difeable de cada numero
# equivocado que el paper imprimio jamas
*.antes_*
*.bak_*
*.canonico_previo
*.previo

# figuras que el notebook escribe al vuelo: ya van embebidas dentro del .ipynb,
# publicarlas sueltas en la raiz duplica el contenido y ensucia el arbol
nb_fig*.png
